<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication07">

    <!-- 震动 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ScheduleDetailActivity"></activity>
        <receiver android:name=".ScheduleDetailActivity$AlarmReceiver" >
            <intent-filter>
                <action android:name="com.example.myapplication07.ScheduleDetailActivity.AlarmReceiver" />
            </intent-filter>
        </receiver>
        <activity android:name=".ScheduleActivity" android:exported="true"/>
        <activity android:name=".TabHostActivity" android:exported="true"/>
        <activity android:name=".TabButtonActivity" android:exported="true"/>
        <activity android:name=".TabFirstActivity" android:exported="true"/>
        <activity android:name=".TabSecondActivity" android:exported="true"/>
        <activity android:name=".TabThirdActivity" android:exported="true"/>



    </application>

    <uses-permission android:name="android.permission.VIBRATE" />

</manifest>